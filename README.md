Демонстрация запроса в дадату

Для выполнения запроса выполняем:

```bash
DADATA_HOST=<host> DADATA_TOKEN=<token> php query.php
```

Пример:

```bash
DADATA_HOST=https://suggestions.dadata.ru DADATA_TOKEN=blah php query.php
```